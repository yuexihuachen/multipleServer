version: "3.3"
services:
    nodeserver:
        build:
            context: ./app
        ports:
            - "5000:5000"
        volumes:
            - /home/guozishu/localDb:/usr/src/document/server/localDb
    nodeserver1:
        build:
            context: ./pdfBooks
        ports:
            - "5001:5001"
        volumes:
            - /home/guozishu/pdfbooks:/usr/src/wrokspace/pdfs
    nginx:
        restart: always
        build:
            context: ./nginx
        ports:
            - "80:80"
            - "8080:8080"
